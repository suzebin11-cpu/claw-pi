name: Desktop Beta

on:
  workflow_dispatch:

permissions:
  contents: write
  actions: write

concurrency:
  group: desktop-beta
  cancel-in-progress: false

jobs:
  build:
    uses: ./.github/workflows/desktop-build.yml
    with:
      environment: nexu-prod
      sentry_env: "prod"
      cloud_url: "http://47.108.215.151:9080"
      link_url: "https://yunwu.ai"
      update_feed_url: ""
      build_source: "beta"
      release_tag: desktop-beta
      release_name: "Nexu Desktop Beta"
      channel: "beta"
    secrets: inherit
