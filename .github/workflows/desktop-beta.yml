name: Desktop Beta

on:
  workflow_dispatch:
  schedule:
    - cron: "0 18 * * *"

permissions:
  contents: write
  actions: write

concurrency:
  group: desktop-beta
  cancel-in-progress: false

jobs:
  build:
    uses: ./.github/workflows/desktop-build.yml
    with:
      environment: nexu-prod
      sentry_env: "prod"
      cloud_url: "https://api.clawpi.app:9443"
      link_url: "https://api.clawpi.app:9443"
      update_feed_url: ""
      build_source: "beta"
      release_tag: desktop-beta
      release_name: "Nexu Desktop Beta"
      channel: "beta"
    secrets: inherit
